package examples.algorithms.sorting.merge;

public class Main {

    public static void main(String[] args) {
        int[] list = {4, 2, 9, 1, 6, 0, 3, 7};
        Merge merge = new Merge();
        merge.merge(list);
    }
}
